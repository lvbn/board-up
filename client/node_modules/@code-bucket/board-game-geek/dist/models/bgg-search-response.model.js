import { BggSearch } from './bgg-search.model';
var BggSearchResponse = /** @class */ (function () {
    function BggSearchResponse(data) {
        this.total = Number.parseInt(data.items._attributes.total);
        var items = data.items.item
            ? Array.isArray(data.items.item)
                ? data.items.item
                : [data.items.item]
            : [];
        this.items = items.map(function (item) { return new BggSearch(item); });
    }
    return BggSearchResponse;
}());
export { BggSearchResponse };
//# sourceMappingURL=bgg-search-response.model.js.map